Hola{% if user.get_full_name %}, {{ user.get_full_name }}{% endif %}:

Recibimos una solicitud para restablecer la contraseña de su usuario "{{ user.get_username }}" en {{ site_name }}.

Abra el siguiente enlace en su navegador (caduca en unos días por seguridad):

{{ protocol }}://{{ domain }}{% url 'password_reset_confirm' uidb64=uid token=token %}

Si usted no solicitó este cambio, ignore este mensaje. Su contraseña no se modificará hasta que use el enlace anterior.

Saludos,
{{ site_name }}
